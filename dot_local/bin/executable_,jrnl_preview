#!/usr/bin/env bash

set -Eeuo pipefail

function show() {
  printf "\n$(jrnl -on "${1}" -n 1 --format text)"
}

for date in "$@"
do
  show "${date}" | \
    bat \
      --highlight-line 2 \
      --language md \
      --plain
done
