#!/usr/bin/env bash

set -euo pipefail

softwareupdate --agree-to-license --install --all
softwareupdate --history

"${HOME}"/.local/homebrew/bin/brew update
"${HOME}"/.local/homebrew/bin/brew upgrade

